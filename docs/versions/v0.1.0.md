# v0.1.0 开发日志

> 按 [agent-rules / workflows/rapid-versioning.md](https://github.com/jeffliulab/agent-rules) 的轻量模式：
> 同一天追加，不重复创建。每天一个二级标题，下面三栏：完成 / 决策 / 遗留。

**目标**：14 天 calendar 内交付一个可演示的"金融版 Cursor" v0.1.0 —— 三栏 IDE 形态、个股模块覆盖 A 股 + 美股、Citation drawer。

详细任务清单见 [`../NEXT_STEPS.md`](../NEXT_STEPS.md)。

---

## 2026-04-24

### 完成

**Day 1 — 工程地基**
- 建立版本追踪三件套：`VERSIONS.md` + `docs/NEXT_STEPS.md` + `docs/versions/v0.1.0.md`
- 写 `AGENTS.md`（指向 `~/Local_Root/agent-rules/agent-rules/`，项目类型 = fullstack-app）+ `CLAUDE.md` shim
- 搬调研报告 `.claude/plans/agent-...-{ab70975233323be79,aa0318ad68af311d1}.md` → `docs/research/{competitor-landscape,data-sources}.md`，加来源元信息
- 重写 `README.md`（英文）+ 新增 `README_zh.md`
- 废弃 `docs/PRD-draft.md`（旧 M2 合规审查方向）→ 重写为 `docs/PRD.md`（金融版 Cursor 三板块方向）
- 新增 `docs/architecture.md`（三栏 + 数据流 + 组件分工）
- `src/fin_pilot/` → `backend/` 搬迁，按 plan §5 目录布局：
  - `llm/*` → `backend/llm/*`（drop-in，仅改 `__init__.py` docstring）
  - `analysis/*` → `backend/services/analysis/*`（M1 资产，v0.1 不接但留路径）
  - `agents/*` → `backend/services/analysis/agents/*`
  - `agents/prompts/*.j2` → `backend/data/prompts/analysis/*`（PackageLoader 路径同步更新）
  - `data/*` → `backend/repositories/market/*`
  - `config.py` → `backend/config.py`（保留旧字段，v0.1 实际用的字段在 Day 2-4 增补）
- 写 `pyproject.toml`（fastapi / uvicorn / pydantic / akshare / yfinance / langchain / langgraph / anthropic / openai / pyyaml / jinja2）
- 写 `.env.example`（OPENAI_API_KEY / ANTHROPIC_API_KEY / DEEPSEEK_API_KEY / LLM_PROVIDER / LOG_LEVEL）

### 决策

- **拒绝 v0.1 内做 PDF.js 高亮**：iframe 加载原文 URL 即可；PDF.js 留到 v0.5
- **决定 v0.1 上 LangGraph**：即便 v0.1 只有一个"个股分析助手" agent，也用 LangGraph 起步，给 v0.2+ 多 agent 留路径，对齐 ai-hedge-fund / TradingAgents 的架构
- **决定不用 Pydantic v1**：直接用 v2，因 LangGraph + Vercel AI SDK 都假设 v2

### 遗留

- 虚拟环境 `conda create -n fin-pilot python=3.11` 待用户在本地执行（plan 决定了 venv 工具，agent 不代为创建）
- `from backend.llm import get_llm` 烟测 待 venv 就绪后跑

---

> 后续每天追加新的二级标题（如 `## 2026-04-25`）；不要修改历史日期项。
